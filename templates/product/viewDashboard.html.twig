{% extends 'base.html.twig' %}

{% block body %}
	<div class="dashboard-section">

		<div class="container">
			<h2 class="mb-4 text-center">Tableau de Bord Admin</h2>
			<div class="card shadow-lg mb-4">
				<div class="card-body">
					<table class="table table-striped">
						<thead class="thead-dark">
							<tr>
								<th>Titre</th>
								<th>Action</th>
							</tr>
						</thead>
						<tbody>
							{% for product in products %}
								<tr>
									<td>{{ product.title }}</td>
									<td>
										<a href="{{ path('admin_edit_product', {id_product: product.id}) }}"
                                         class="btn btn-info btn-sm">Éditer</a>
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
					<a href="{{path('admin_add_product')}}" class="btn btn-primary btn-lg">Ajouter un Produit</a>
				</div>
			</div>
		</div>
	</div>
    

{% endblock %}
