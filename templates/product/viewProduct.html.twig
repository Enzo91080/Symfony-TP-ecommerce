{% extends 'base.html.twig' %}

{% block title %}
	{{ article.title }}
{% endblock %}

{% block body %}

	<div class="container my-5">
		<div class="row">
			<div class="col-md-6">
				<img src="{{article.picture}}" alt="{{article.title}}" class="img-fluid rounded">
			</div>
			<div class="col-md-6">
				<h2>{{article.title}}</h2>
				<p class="text-muted">{{article.price}} €</p>
				<p>{{article.description}}</p>
				<a href="cart.php?action=add&id=<?php echo $product_id; ?>" class="btn btn-success btn-lg">Ajouter au Panier</a>
			</div>
		</div>
	</div>

{% endblock %}
